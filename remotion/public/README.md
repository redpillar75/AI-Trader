Drop your source video here as `input.mp4`.

`remotion/src/Root.tsx` reads this file's real duration/resolution automatically
(via `calculateMetadata` + `@remotion/media-utils`), so there's nothing else to
configure — just add the file and run `npm start`.
